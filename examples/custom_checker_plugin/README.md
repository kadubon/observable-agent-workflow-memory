# Custom Checker Plugin

Implement `observable_agent_workflow_memory.ports.checker.Checker`, then expose
it through the `oawm.checkers` entry-point group.

The default tests include contract helpers for checker behavior.

